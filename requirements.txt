# Tournament/runtime baseline from the course handout.
numpy>=2.2.6
scipy>=1.15.3
scikit-learn>=1.7.2
tqdm>=4.70.0

# Optional GUI. Python 3.13 is recommended because pygame currently has
# reliable prebuilt wheels for it.
pygame>=2.6.1; python_version < "3.14"

# Neural-network runtime for DagobertDuckDQN (CPU-only).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.14.0+cpu; sys_platform != "darwin"
torch==2.13.0; sys_platform == "darwin"
