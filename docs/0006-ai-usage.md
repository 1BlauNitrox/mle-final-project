# 0006 AI Usage and Disclosure

## Course rule

AI may be used to brainstorm, assist with code, and draft report text. The final
code and report must demonstrate that the team performed the main work.
Unreviewed AI drafts are not acceptable.

## Team policy

For every material use of AI:

1. A team member owns the result.
2. The owner verifies technical claims against primary sources or experiments.
3. Generated code receives the same tests and peer review as human-written code.
4. Generated prose is critically edited and rewritten into the team's own
   precise style.
5. The PR explains material AI assistance and the verification performed.
6. No secret, personal, restricted, or unpublished competition information is
   placed into an external AI service.

AI output is a draft or suggestion, never experimental evidence.

## Disclosure log

| Date | Scope | Tool | Human verification and refinement |
| --- | --- | --- | --- |
| 2026-07-25 | Initial repository structure, workflow, requirements summary, AGENTS.md, and documentation drafts | OpenAI Codex | Team must review course-summary accuracy, adapt wording and ownership, run CI, and approve through PR review before merge. |
| 2026-08-16 | Task 1 baseline scope, metric definitions, completion thresholds, and repository contract test for issue #21 | OpenAI Codex | Team must verify that the prospective thresholds and statistical procedure match its intended compute budget, review the wording, run CI, and approve through PR review before merge. |
| 2026-08-19 | Issue #25 experiment metrics, aggregation, runner, plotting pipeline, tests, and documentation | OpenAI Codex | The author reviewed and adapted the generated design and code, traced the framework statistics flow, ran Ruff and the unit-test suite, exercised successful and failed runner paths, inspected normalized and aggregated outputs, visually reviewed regenerated plots, and verified that raw run artifacts remain ignored. The pipeline smoke run was treated only as an infrastructure check and not as experimental evidence. |
| 2026-08-21 | Review fixes for the experiment metric pipeline in issue #25: learning-metric transport, observed-agent isolation, configuration fingerprinting and dirty-state preservation, step semantics, complete action accounting, stored-data validation, coin-efficiency aggregation, tests, and documentation | OpenAI Codex | Team must inspect the callback contract and metric definitions, verify configuration recovery and multi-agent behavior, run the full validation suite, and obtain peer approval before merge. |
| 2026-08-26 | task 1 Q-learning Agent tests and documentation | OpenAI Codex | The author reviewed and adapted the generated material, ran the complete test and lint suite, executed training and evaluation smoke runs, inspected the resulting metrics and plots, and verified that evaluation did not modify the model artifact. |
| 2026-08-28 | Issue #34 DQN agent architecture, feature and reward contracts, replay buffer, online and target networks, Bellman updates, seeded policy, checkpoint persistence, framework callbacks, tests, agent card, and review fixes | OpenAI Codex | The author manually integrated and reviewed the suggestions, corrected implementation mistakes through focused tests, and retained ownership of the design. Ruff, compilation, the complete unit suite, seeded training and evaluation smoke runs, resume and determinism checks, checkpoint immutability, latency and memory measurements, packaging, clean-framework compatibility, and official Docker compatibility were completed and recorded in PR #38. Review fixes added whitespace validation and bounded DQN and Docker smoke coverage to CI; the imported Dockerfile was pinned to the published Miniconda Python 3.13 image after its moving base changed to Python 3.14 and could no longer install the supplied TensorFlow dependency. The Docker check installed the agent-local PyTorch constraint, evaluated against three random agents, verified one CPU thread and checkpoint immutability, and repeated evaluation from a clean packaged-agent framework export. Non-author approval remains required before merge; no AI output or smoke run is treated as performance evidence. |
| 2026-09-01 | automated experiment runner | OpenAI Codex | The author reviewd and adapted the generated code. |
| 2026-09-02 | task 1 Q-learning Agent experiments documentation | OpenAI Codex | The author reviewed and adapted the generated material. |
| 2026-09-01 | Issue #41 prospective DQN Task 1 development-baseline configuration, training diagnostics, serial five-run launcher, failure retention, and tests | OpenAI Codex | The owner must review the fixed seeds, thresholds, source fingerprint, and blocker assessment; inspect the launcher and metric semantics; run the complete validation suite; and obtain non-author review before scientific training. The launcher deliberately refuses dirty, resumed, source-mismatched, or seed-colliding runs. No training result or performance claim was generated by AI assistance. |
| 2026-09-01 | Issue #41 evaluation orchestration, raw decision-time retention, deterministic repeats, aggregation, figures, and result documentation | OpenAI Codex | The owner reviewed the registered seeds and retained training failure, executed all scientific jobs locally, and must review the generated tables and prose plus obtain non-author approval. Metrics were computed from retained raw framework outputs; AI output was not used as experimental evidence. The unavailable tabular pairs are reported as missing rather than reconstructed or imputed. |
| 2026-09-02 | PR #49 review remediation: clean evaluation-artifact staging, compact evidence export and verification, removal of the stale tabular comparison, tests, and documentation | OpenAI Codex | The owner must inspect the provenance design and generated evidence, verify the rerun against the immutable artifact hashes, run the full validation suite, and obtain non-author review. All scientific values remain derived from retained framework outputs; AI output is not experimental evidence. |
| 2026-09-03 | Issue #60 CI runtime analysis, event-aware path classification, optional-job parallelization, tests, and workflow documentation | Anthropic Claude Code and OpenAI Codex | Claude identified duplicate PR runs and the uncached serial Docker critical path. Codex verified the claims against GitHub Actions timings, branch protection, and the current workflow, then refined the proposal to handle `main` push ranges and all relevant runtime paths. The owner must review the classification boundary, validate both skipped and triggered paths in GitHub Actions, inspect security permissions, and obtain non-author approval before merge. |
| 2026-09-03 | Issue #40 Task 1 baseline freeze, training-protection design, integrity tests, clean-package validation, and documentation | OpenAI Codex | The owner independently selected the frozen model from committed development evidence, reproduced its SHA-256 checksum and byte size, verified its provenance and schema values, reviewed and adapted the suggested code and documentation, and ran Ruff and the complete test suite. Clean-package CI validation and non-author approval must be completed before merge. AI output was not treated as experimental evidence. |
| 2026-09-03 | Issue #58 DQN movement-shaping experiment: shaping port from the tabular agent, single-run retry capability, paired bootstrap module, evidence-module profile parameterisation, checkpoint-replace retry fix, and the experiment record | Claude (Anthropic) | The owner reviewed each change, ran the full suite, Ruff and compileall, and verified claims against retained outputs rather than assertions: the bootstrap was validated by reproducing issue #41's published aggregate and per-model fractions exactly from committed evidence, and evidence verification was tamper-tested on both the committed-table and checksum paths. The negative result is reported as measured; no threshold, seed or criterion was changed after results were seen, and the run-5 retry was documented prospectively before evaluation ran. |
| 2026-09-03 | Issue #65 behavior-preserving tabular Task 2 successor, artifact-migration script, differential tests, agent documentation, architecture update, and clean-package CI | OpenAI Codex | The owner manually integrated and reviewed the suggestions, verified the parent and successor artifact checksums and byte identity, ran Ruff and the focused differential tests, inspected the packaged agent contents. No Task 2 training or performance evidence was generated by AI assistance. |
| 2026-09-04 | Issue #78 CPU-only PyTorch dependency correction, cross-platform requirement markers, dependency contract test, CI runtime verification, and PR documentation | OpenAI Codex | The author must review the dependency policy and platform markers, verify a clean requirements installation resolves the official CPU distribution without CUDA runtime packages, run the repository checks, and obtain peer approval before merge. |
| 2026-09-03 | Issue #42 prospective candidate-selection rule, Task 1 DQN baseline freeze, compact-artifact export script, training-protection guard, freeze tests, CI rework, and documentation | Claude (Anthropic) | The selection rule was posted to issue #42 before the freeze ran, mechanically drawn from issue #58's already-committed evidence. The owner must verify the frozen checkpoint reproduces run-02's network exactly (checked programmatically by the export script and by a dedicated freeze test), run the full suite and Ruff, and obtain non-author review; this issue is assigned to a teammate who has not yet reviewed this work. No new training was run and no threshold or criterion was changed after results were seen. |
| 2026-09-04 | PR #72 review remediation: a distinct evaluation-only checkpoint schema (removing, not resetting, the target network, optimizer, replay, and RNG state), source-checkpoint SHA-256 and size verification in the export script, removal of the now-impossible resumable-checkpoint write from the frozen agent's unreachable training path, the re-exported artifact, and updated tests and documentation | Claude (Anthropic) | Written in response to teammate Waffelmanufaktur's CHANGES_REQUESTED review, which found the previously exported checkpoint structurally resumable and the source checkpoint unverified by checksum. The owner must verify the re-exported artifact still reproduces run-02's network exactly (checked programmatically by the export script's self-check and by the freeze tests, including new tests for source-checksum and size rejection), run the full suite and Ruff, and obtain the same teammate's re-review. No new training was run and no selection or threshold was changed. |
| 2026-09-03 | Issue #43 behavior-preserving DQN Task 2 successor (`DagobertDuckDQNTask2`), checkpoint-migration script, differential tests, features-package split, agent documentation, and CI | Claude (Anthropic) | The owner must verify the migrated checkpoint is byte-identical to the frozen parent (checked programmatically) and that the feature and network split changes no returned value, run the full suite, Ruff and compileall, and obtain non-author review. Mirrors the pattern issue #65 established for the tabular successor. No Task 2 training or performance evidence was generated by AI assistance, and this branch stacks on the unreviewed issue #42 freeze. |
| 2026-09-03 | Issue #44 DQN Task 2 capability: bomb/crate/danger feature module, six-action network and checkpoint migration, Task 2 rewards, event-count diagnostics, a revised hyperparameter set, migration and feature tests, and documentation | Claude (Anthropic) | The owner directed the hyperparameter revision (citing issue #58's training-vs-evaluation gap as the concrete motivation for the epsilon schedule) and must independently verify the migration preserves the parent's weights exactly (checked programmatically), review the danger/escape geometry against the framework's own blast rules, run the full suite, Ruff and compileall, and obtain non-author review. Smoke evidence (training runs, a classic evaluation, and a coin-heaven regression selecting BOMB zero times) is disclosed as integration evidence only; no scientific Task 2 training or performance claim is made, and this branch stacks on the unreviewed issue #42 and #43 work. |
| 2026-09-04 | Issue #44 follow-up after an unregistered overnight development run on `loot-crate` (129,091 episodes): fixed a real bug where `escape_after_bomb` never added a hypothetical bomb before checking for an escape, removed the inherited-but-unexamined `MOVED_TOWARDS_COIN`/`MOVED_AWAY_FROM_COIN` shaping, and added `SAFE_BOMB_PLACED`/`UNSAFE_BOMB_PLACED` reward shaping | Claude (Anthropic) | The owner caught the unexamined-inheritance issue directly (asked why coin-movement shaping was still present after issue #58's inconclusive result) and directed the reward redesign; investigating it surfaced the escape-feature bug independently. The owner must independently review the fix and the new reward's derivation, confirm the regenerated checkpoint's checksum, run the full suite, Ruff and compileall, and obtain non-author review. The overnight run's observations (90% self-kill rate, Task 1 invalid-action rate rising from ~0% to 59%) are disclosed as the motivation for these changes, not as a benchmark for future runs -- the pre-fix agent was not re-evaluated after the fix. No scientific Task 2 claim is made. |
| 2026-09-04 | Issue #44 second follow-up after a second unregistered development run showed Task 1 retention recovered but Task 2 settling into a passive local optimum (rising loot-crate death rate, 43% WAIT, lopsided movement); reduced `SURVIVED_ROUND` and strengthened `WAITED` | Claude (Anthropic) | Claude proposed the diagnosis (an oversized `--rounds` against a schedule tuned for 10,000 episodes, plus a reward imbalance favoring passive survival) and the owner directed the fix. Both contributing factors are stated as a diagnosis from one run's observations, not a proven cause, and the movement-bias observation is disclosed as unresolved rather than root-caused. The owner must independently review the reasoning, confirm the regenerated checkpoint's checksum, run the full suite, Ruff and compileall, and obtain non-author review. No scientific Task 2 claim is made. |
| 2026-09-04 | Consolidated the three exploratory Task 2 development runs into `experiments/2026-09-04-dqn-task2-capability-development/` (binned training-curve data, figures, honest interpretation including an unresolved training-vs-evaluation survival gap) | Claude (Anthropic) | Claude drafted the record from retained run data (the third run's full per-episode CSV; the first two runs' numbers were already reported in chat and PR comments, not re-derived). Claude initially also drafted a specific follow-up experiment hypothesis and protocol (issue #77); the owner asked to design and think through the next experiment's variables independently instead, so #77 was closed and the record's decision section was trimmed to state the known invalid-action-rate limitation as a fact without prescribing what to test next. The owner must review the record for accuracy. No new training was run for this entry; no scientific claim is made. |
| 2026-09-04 | PR #74 review remediation: framework-accurate danger intervals, reachable crate-target BFS, frozen-parent lineage refresh, separation of post-hoc reward tuning, disclosure of the fourth exploratory run, and restoration of Task 2 training setup after rebase | OpenAI Codex | Codex traced timer semantics against `environment.py`, implemented and tested the two reviewer-requested geometry fixes, restored the original reward defaults so unregistered outcomes do not select shipped values, and recorded the retained run-4 metadata without treating it as evidence. A GitHub smoke then exposed that conflict resolution had retained #73's evaluation-only callback setup; Codex restored the explicit train/evaluate setup split and added a regression test before repeating the real three-round smoke successfully. The owner must review the changes, preserve the stashed trained checkpoint until intentionally archived, and obtain the reviewer's approval. Ruff and the full test suite must pass before merge. |
| 2026-09-05 | PR #74 re-review remediation: `safe_escape_exists` now models bomb occupancy from the same bomb list that builds the danger map, at every BFS step rather than only the first, with a wait exemption so an agent is not evicted from a tile it just placed its own bomb on | Claude (Anthropic) | The owner asked Claude to fix the reviewer's remaining finding directly. Claude hand-derived a minimal field where the buggy code would report a false escape (crossing a still-live bomb after waiting) and verified programmatically, by temporarily reverting just the new occupancy check against the new regression tests, that both new tests genuinely fail without the fix and fail again (differently) without the wait exemption specifically, before restoring the real fix. The owner must independently review the fix and obtain the reviewer's approval. Full suite, Ruff, and `compileall` pass locally; no training or performance claim is made. |
| 2026-09-04 | PR #72 retained-source remediation: public release archival of the selected run-02 source checkpoint, exact release locator in the manifest and freeze contract, independent-download reproduction instructions, and provenance tests | OpenAI Codex | The owner explicitly authorized publishing the previously ignored 862,842-byte training checkpoint in the public repository. Codex verified the local source hash before upload, checked GitHub's release-asset digest and size, downloaded a fresh copy, reran the restricted export and Q-value self-check, and reproduced the committed evaluation artifact's exact size and SHA-256. A non-author must repeat that verification from the release asset and approve the PR. No training, candidate selection, threshold, or scientific result changed. |
| 2026-09-05 | Issue #50 YAML run-plan schema, staged matrix runner, deterministic resume and artifact isolation, example plans, tests, CI expansion check, and documentation | OpenAI Codex | The owner approved YAML v1, bounded smoke resources, and 1BlauNitrox as non-author reviewer. The implementation reuses the existing metrics pipeline, was checked with focused tests and real one-round Task 1-3 integration jobs, and must receive full-suite/CI verification and non-author review. Smoke outputs are infrastructure checks, not performance evidence. |
| 2026-09-04 | Issue #75 aligned `docs/0004-experimentation-protocol.md` with the repository's one-main-variable rule and documented the interaction/factorial exception | Claude (Anthropic) and OpenAI Codex | Claude checked the assignment and drafted the original broader bundled-experiment wording. In response to review, Codex narrowed the exception to prospectively stated interactions with planned factors and ablations, retained the distinction between exploratory diagnosis and scientific evidence, and resolved the disclosure conflict with current `main`. The owner must verify policy alignment and obtain the reviewer's approval. Documentation-only change; no code or experimental result is affected. |
| 2026-09-05 | Issue #46 prospective Task 2 DQN run plans, seed-compatible migration starts, Task 2 metric instrumentation, deterministic-repeat analysis, tests, and server execution documentation | OpenAI Codex | The owner must review the scientific thresholds and seed populations, inspect the dry-run matrices, approve the compute budget, and review all code and test evidence before the first scientific run. No training result or performance claim was generated. |
| 2026-09-05 | Issue #82 correction for hidden-coin denominator and evaluation-only rerun mode | OpenAI Codex | The owner reported an analysis failure from an executed Issue #46 run. Codex inspected the raw CSV evidence, identified a zero denominator for hidden coins, and added the issue-linked correction, regression test, and checkpoint-reusing evaluation-only path. The pre-fix raw outputs remain preserved and invalid for collection-fraction analysis; no result was discarded. |
| 2026-09-07 | Issue #87 multi-step escape-continuation features, bounded trajectory semantics, Issue #85 checkpoint migration, persisted treatment/run-plan switch, tests, latency smoke, and agent documentation | OpenAI Codex | The owner must independently review the horizon accounting, terminal-safe rule, bomb/opponent occupancy, overlap timing, feature-off neutral inputs, migration provenance, and treatment compatibility checks. The implementation was verified against the unchanged framework blast geometry, an exhaustive bounded trajectory oracle, focused unit tests, Ruff, compilation, smoke games, and latency measurements. No scientific training or performance claim was generated. |
| 2026-09-07 | Issue #88 protected Task 1 replay partitions, deterministic persistence/resume, staged treatment wiring, evaluation-only export, tests, and documentation | OpenAI Codex | The owner must review the fixed 2,000/8,000 partition semantics, verify the quota and fallback tests, run the required lint, unit, compile, and bounded integration checks, and obtain non-author review. No scientific training run or performance claim was generated. |
| 2026-09-07 | Issue #107 prospective 2x2 DQN Task 2 campaign, six immutable run plans, seed-collision and artifact preflight, treatment-provenance correction, paired factorial analyzer, multiplicity-adjusted efficacy gates, mechanical cell/artifact selection, aggregate resource enforcement, authorization-bound job evidence, portable reviewed-plan comparison, tests, and documentation | OpenAI Codex | The owner must verify the fixed seed populations, 200,000/5,120 episode budgets, four efficacy decisions, non-regression guards, Issue #46 absolute gates, source lineage, persisted CPU/wall/memory ceilings, process-tree termination, authorization/resource and per-job reviewed-commit binding, parent-path-only portability normalization, and selection rule; run all validation; obtain non-author approval; and explicitly authorize compute for the reviewed clean commit before execution. No training, evaluation result, performance claim, or compute authorization was produced by AI assistance. |
| 2026-09-06 | Issue #45 tabular Task 2 successor: bomb and crate features, six-action Q-learning, frozen-parent initialization, schema-validated persistence, native Task 2 rewards, episode diagnostics, tests, artifact metadata, documentation, and bounded framework smoke validation | OpenAI Codex | The author manually reviewed and adapted the suggestions, implemented the changes, ran Ruff and the complete test suite, and exercised a bounded training smoke run in an isolated temporary repository copy.|
| 2026-09-06 | Issue #85 migration correction: zeroed Task 2-only input weights, regression/differential tests, versioned artifact, and prospective evaluation-only protocol | OpenAI Codex | The owner must inspect the function-preservation and gradient tests, reproduce the artifact checksum from the frozen parent, review the bounded protocol before execution, run local and CI validation, and obtain 1BlauNitrox's non-author approval. No training or evaluation result was generated by AI assistance. |
| 2026-09-06 | Issue #85 evidence-provenance remediation: runner fingerprint alignment, regression test, and rejected-execution documentation | OpenAI Codex | Codex identified that the run plan excluded a disposable staged evaluation checkpoint while the generic runner included it in per-run agent digests, making the required analyzer reject the retained first execution. The owner must review the narrow exclusion, verify the focused tests and repeat the approved matrix from a clean checkout after peer approval. The invalid first execution is retained and no scientific conclusion is drawn from it. |
| 2026-09-06 | Issue #93 evidence policy by pull-request type across the Definition of Done, experiment protocol, repository architecture, contributor guidance, `AGENTS.md`, PR template, and contract test | OpenAI Codex | Codex converted the team's review precedent into a conditional evidence policy: prospective protocols do not require results, completed experiments and model freezes require claim-checkable evidence, large required artifacts need durable retrieval metadata, and incomplete records remain open through `Refs`. The owner must review the policy wording against the intended workflow, run the repository checks, and obtain non-author approval. Documentation-only change; no experiment, artifact, or scientific conclusion is modified. |
| 2026-09-06 | Issue #86 framework-legal action-mask implementation, replayed next-state masks, run-plan treatment recording, tests, and server-readiness review | OpenAI Codex | The owner must verify the mask remains exactly aligned with `environment.py`, review the prospective protocol and resource budget, run the complete validation suite, and obtain non-author review from 1BlauNitrox. No training or performance claim was generated. |
| 2026-09-07 | Issue #114 immediate useful-bomb reward for the tabular Task 2 agent: opt-in reward and persistence support, run plans, tests, prospective experiment record, autonomous control/candidate execution, diagnostic-recorder correction and deterministic training replays, paired-bootstrap analysis, figures, interpretation, and immutable evidence packaging | OpenAI Codex | At the owner's explicit request, Codex implemented and executed the experiment through the completed evaluation without a separate pre-training approval pause. Codex identified that the original generic CSV schema discarded the agent's `useful_bombs` diagnostic, corrected the recorder, and ran training-only diagnostic replays; all ten replay model hashes matched the original models exactly. The registered result rejects the `+1.0` reward because its small Classic collection improvement was not statistically reliable, only three of five replicas improved, and the Classic self-kill rate rose substantially, despite increased useful-bomb placement and crate destruction. The owner must independently review the implementation, evidence, analysis, and interpretation and obtain non-author approval before merge. Confirmation seeds were not used. |
| 2026-09-07 | Issue #103 reward-shaping treatments: proposed the arm structure (two isolated treatments, not one bundled arm) and the decision rule, implemented the `reward_variant` run-plan field (mirroring `action_masking`), config.py's variant resolution, train.py's new `SAFE_BOMB_PLACED`/`UNSAFE_BOMB_PLACED` bomb-safety event, run plans for all three arms, and tests; also found and fixed a pre-existing bug in persistence.py's reward-mismatch checkpoint guard that would have crashed two of the three arms immediately | Claude (Anthropic) | The owner asked Claude to propose reward values from #46/#86's documented evidence and prepare the experiment fully for execution. Claude found that the survival_rebalance and safety_bomb numeric values were not new guesses but exactly what an earlier, reverted, unregistered development branch had already tried uncontrolled -- reused here rather than inventing a third number -- and chose the arm structure and decision-rule thresholds itself, flagged as proposals for the owner to confirm, not team-ratified. While testing, Claude found that persistence.py's checkpoint reward-mismatch guard rejected any fresh migration to a different reward_variant unconditionally, unlike the equivalent action_masking guard which already tolerates it for an untrained checkpoint; reproduced the crash, fixed it by relocating the check into callbacks.py with the same fresh-migration leniency, and re-verified end-to-end through a real subprocess run (not just unit tests) that a mismatched, already-trained checkpoint still correctly rejects. Full test suite (601 tests) and `ruff check` pass; all three plans verified with `--dry-run`. No training was run and no result is claimed. |
| 2026-09-07 | Issue #103 result analysis: wrote `training/analyze_issue103_dqn_task2_reward_shaping.py` against the owner's completed run and replaced one proposed decision-rule gate that was not computable from evaluation data | Claude (Anthropic) | The later PR review established that the rule had neither received prospective non-author approval nor remained unchanged. The retained measurements are therefore exploratory observations only; they do not adopt or reject a reward treatment and cannot close #103. |
| 2026-09-07 | Issue #103 evidence commitment: wrote `training/export_evidence.py` and a `--verify-from-evidence` mode for the analyzer, in response to Waffelmanufaktur's review that the result could not be independently verified without the ~4.4 GiB raw output tree | Claude (Anthropic) | The owner explicitly decided not to host the raw output tree centrally (mostly redundant per-job source snapshots and framework_stats.json dumps, not evidence) and asked Claude to address the review without it. Claude committed the actual per-episode evidence the analyzer reads (~19 MiB, not 4.4 GiB) plus a fingerprint/provenance manifest, and added a mode that recomputes result.json from only that committed evidence and diffs it against the committed one -- confirmed to print "MATCHES committed result.json". The owner must review whether this satisfies the review's verifiability concern, since it does not restore access to the raw tree, only makes the compact result independently reproducible from what is committed. Full test suite (607 tests) and `ruff check` pass. |
| 2026-09-08 | PR #104 review remediation: resolved the reward-variant conflicts with the merged escape-feature and protected-replay contracts, made CSV exports platform-stable, enforced manifest byte-size and SHA-256 validation, regenerated the compact evidence, and narrowed the completed run to an exploratory result | OpenAI Codex | Codex reproduced the three invalid manifest records, regenerated them from the preserved `C:\run103` outputs, and added clean-checkout and tamper regressions. It also found that every retained job records a dirty execution tree and documented that provenance limitation. The owner must inspect the conflict resolution and wording and obtain renewed non-author approval. No training was run and no experimental observation was changed or discarded. |
| 2026-09-06 | Issue #97 staged-curriculum comparison for Task 2 DQN: registered hypothesis, direct-classic run plans (masked and unmasked), and blocking-dependency documentation | Claude (Anthropic) | The owner specified the hypothesis, comparison arms, seeds, and dependency structure in chat; Claude drafted the issue and run plans from that specification without inventing numeric decision criteria, which remain open for the owner to set. Confirmed both plans dry-run cleanly on current `main`, and that `action_masking` is silently ignored there until PR #95 merges -- documented as a blocking dependency, not silently worked around. No training was run and no result is claimed. |
| 2026-09-07 | Issue #97 finalization for execution: deleted the masked direct-classic plan, updated `config.yaml`/README to reflect Issue #86's actual decision (masking rejected, unmasked arm retained), and halved the resource-ceiling estimate to match the smaller (one-arm, not two) scope | Claude (Anthropic) | The owner asked for the experiment to be fully execution-ready behind a single command, given #86's decision was now known. Claude did not set the still-open decision-rule thresholds, per the owner's standing preference. The owner must review the deletion and resource-ceiling estimate before running. No training was run and no result is claimed. |
| 2026-09-07 | Issue #97 partial result analysis: wrote `training/analyze_issue97_dqn_task2_curriculum.py` against the owner's own completed direct-arm run and wrote up the "Result and decision" section, disclosing that the registered direct-vs-staged comparison could not be computed | Claude (Anthropic) | The owner ran the direct arm themselves and asked Claude to analyze as far as possible and finish the README. Claude found that Issue #86's raw archive (needed for the staged arm) was not present on the machine that ran the direct arm, and wrote the analyzer to degrade to a direct-arm-only descriptive result rather than fail outright or fabricate a comparison, with the gap explicitly marked in both the script's own output and the README/config's "Known gaps". The owner must locate #86's raw archive (or accept the gap) before the registered comparison can be completed. Full test suite and `ruff check` pass. |
| 2026-09-07 | Issue #97 review correction: after Waffelmanufaktur's non-author review requested changes, computed both arms' epsilon-at-`classic`-entry (arm A from raw data, arm B analytically from the documented schedule, satisfying acceptance criterion 3 without the missing archive), changed `Closes #97` to `Refs #97`, and documented that acceptance criterion 4 (decision rule before training) was violated for arm A and cannot be fixed retroactively | Claude (Anthropic) | Claude's own prior write-up had treated the direct-arm-only result as a documented "known gap" sufficient for review, missing that #97's own specific acceptance criteria (drafted by Claude from the owner's design) required the decision rule before training and the epsilon disclosure -- neither of which the initial write-up satisfied. The owner must decide how #97's eventual decision rule accounts for arm A's now-visible numbers (see the README's Process note); this PR still does not close #97. Full test suite and `ruff check` pass. |
| 2026-09-07 | Issue #97 evidence commitment: wrote `training/export_evidence.py` and a `--verify-from-evidence` mode for the analyzer, addressing the same raw-data verifiability concern Waffelmanufaktur raised on PR #104 | Claude (Anthropic) | The owner decided not to host the ~1.5 GiB raw output tree centrally and asked Claude to address verifiability without it. Claude committed the actual per-episode evidence the analyzer reads (~6.1 MiB) plus a fingerprint/provenance manifest, and added a mode that recomputes result.json from only that committed evidence and diffs it against the committed one -- confirmed to print "MATCHES committed result.json". This does not address #97's own still-open acceptance-criteria gaps (see the prior entry); it only makes the direct arm's descriptive result independently reproducible. Full test suite (593 tests) and `ruff check` pass. |
| 2026-09-08 | Issue #97 evidence-integrity fix: after LiliWestermann's non-author review found that `evaluation-episodes.csv`'s committed bytes did not match its own manifest checksum, fixed `_write_plain_csv`/`_write_gzipped_csv` to emit LF line endings directly, added a reusable `verify_evidence_files()` check wired into `rows_from_evidence()`, and regenerated the evidence from the still-present raw run-plan output | Claude (Anthropic) | The reviewer diagnosed the exact root cause (the csv module's default CRLF terminator silently rewritten to LF by `.gitattributes`' `eol=lf` rule at commit time, so the manifest fingerprinted bytes no different from what any clean checkout actually contains) and specified the fix; Claude implemented it, reproduced the reported checksum/size mismatch byte-for-byte against the actual git blob before fixing it, and added regression tests for both the LF-stability property and the new manifest-mismatch guard. Full test suite (629 tests) and `ruff check` pass. This does not address #97's own still-open acceptance-criteria gaps (decision rule, staged-arm comparison; see the 2026-09-07 entries above). |
| 2026-09-07 | Issue #108 Task 3 DQN successor contract, public-opponent features, elimination reward attribution, zero-column migration, run plan, tests, packaging, and CI coverage | OpenAI Codex | The owner must inspect and ratify the feature/reward/migration contract, verify the provisional parent against issue #107's eventual selection, review the bounded smoke outputs as integration-only evidence, run the complete local and CI checks, and obtain non-author approval. No scientific training result or performance claim was generated. |
| 2026-09-08 | Issue #108 feature-count correction: redesigned the eight-value opponent suffix into thirteen values (34 total inputs, not 29) -- four per-direction occupancy flags replacing a single aggregate adjacency count, and second-nearest-opponent direction/distance replacing a single blast-count aggregate | Claude (Anthropic) | The owner flagged the opponent encoding as too thin relative to issue #108's "opponents as obstacles" and multi-opponent (`classic` has up to three) scope, and asked for the redesign. Claude chose the specific 13-feature breakdown and regenerated the migration checkpoint, contract doc, and tests; the owner must review whether this fully satisfies #108's opponent-awareness intent before Task 3 training. Full test suite (605 tests), `ruff check`, `compileall`, and real smoke games (solo and against `peaceful_agent`) pass. No scientific training result or performance claim was generated. |
| 2026-09-10 | Issue #128 compact tabular Task 2 state representation, selectable zero-initialized state contracts, state-visit persistence and diagnostics, optional evaluation-metric transport, preregistered paired run plans, local execution, bootstrap analysis, figures, and result documentation | OpenAI Codex | The owner selected the compact state hypothesis and manually applied, reviewed, and tested the suggested implementation. The protocol and dry-run matrices were verbally approved by `1BlauNitrox` before training. The owner executed both run plans locally; all 2,430 jobs completed and all deterministic repeats matched. Ruff, focused agent, metric, analysis, aggregation, runner, and plotting tests passed, and the generated figures were visually inspected. The full local suite reported 740 passing tests and two unrelated pre-existing DQN artifact-tolerance failures. All scientific values were derived from retained framework outputs using the committed analyzer; AI output was not treated as experimental evidence. The registered negative decision was retained without changing its criteria after observing the results, and confirmation seeds were not used. |

Add entries when AI materially influences agent design, implementation,
experiments, analysis, or report drafting. Minor autocomplete need not be logged
individually.

Issue #107 server-result interpretation (2026-09-09): OpenAI Codex inspected
the downloaded result JSON, tables, authorization and resource records, and
documented the reported contrasts, failed safeguards and mechanical A/r2
selection. Codex subsequently verified 5,180 jobs and twenty final artifacts,
implemented and tested explicit Linux path ordering and execution-versus-analysis
dependency handling, and reproduced the server result exactly from downloaded
episode evidence using the unchanged registered analyzer. A human reviewer must verify the retained
evidence and interpretation before accepting the result. No training was run
and no agent default or artifact was changed.
The subsequent self-review added final archive-member integrity validation
and regression tests, and documented the distinction between disposable local
downloads and server diagnostics. This self-review does not replace approval
by another team member.

- September 11, Issue #91: Codex inspected accepted #107 evidence and incomplete #124 evaluation, proposed one prospective gamma comparison, and implemented tested initialization/orchestration/analysis. No scientific training or performance improvement is claimed; human review and ownership remain required.

- 2026-09-11, Issue #91 / PR #136: Codex corrected the missing training-only
  runner API after an actual server startup failure. The integration test now
  runs both phases and verifies no repeated training. Added a tested, narrowly
  scoped pre-job recovery preserving original resource accounting and failures.
  Earlier green tests did not cover the campaign call; they were insufficient
  evidence for that execution path. No scientific improvement is claimed.
