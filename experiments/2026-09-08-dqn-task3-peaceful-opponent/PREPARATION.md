# Task 3 preparation and launch dependencies

The 39-input successor in #125 / PR #130 retains the complete 26-input Task 2
prefix and the existing thirteen opponent inputs. Both online and target
networks and the parent mask/escape modes are preserved. PR #120's comparison
templates are integrated into that implementation branch.

## Parent options

The final campaign parent depends on #124's registered selection. If its
absolute Task 2 gates fail, record an exploratory continuation. Do not select a
favorable replica or close Task 2 based on Task 3 performance.

#107's A/r2 selection has already been independently verified in merged #122;
it is no longer an unresolved verification dependency. It remains a historical
Task-2-incomplete fallback, not a substitute for the pending #124 selection.
The baseline preparation command and durable artifact locator are in
[the #125 record](../2026-09-10-task3-escape-migration/README.md).

## Matched comparison

The candidate and frozen predecessor receive identical peaceful-opponent
classic and opponent-free classic, coin-heaven and loot-crate suites, each
with primary/repeat passes. Templates currently contain five replicas x
10,000 training episodes, 405 candidate jobs and 80 reference jobs.
Historical retention seeds are reused development seeds, not unseen held-out
confirmation evidence. Finalize and collision-audit the actual execution
population before training.

## Remaining scientific launch gates

1. Bind the chosen parent and both artifact hashes/mode flags explicitly.
2. Agree numerical elimination, score/first-place, self-kill, retention and
   fallback criteria before observing Task 3 results.
3. Implement/test the analyzer against those criteria, including observed-agent
   filtering, deterministic repeats, paired uncertainty and provenance checks.
   Opponent kills are present in raw framework statistics but the current
   normalized CSV does not export them; add/test their retention before a
   scientific campaign relies on compact CSV-only evidence.
4. Enforce and authorize the shared CPU/wall/RAM budget. Task 2's authorization
   does not automatically cover Task 3 training.
5. Review the final immutable execution revision and pass CI.

The coin-collector plan is a template requiring a peaceful-stage selected
parent and its own prospective decision. Do not launch it merely because the
YAML parses. Bounded integration smokes are not scientific results.
